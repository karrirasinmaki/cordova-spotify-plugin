repositories{
  jcenter()
  flatDir{
      dirs 'libs/spotify'
   }
}

dependencies {
   compile(name:'spotify-auth-latest', ext:'aar')
   compile(name:'spotify-player-latest', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
